import {Search, Input, InputsBar} from './UiComponents';

const SearchBar = () => {
    return (
        <InputsBar>
            <Input/>
            <Search/>
        </InputsBar>
    )
}

export default SearchBar;
